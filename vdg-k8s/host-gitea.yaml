ingress:
  enabled: true
  hosts:
    - host: gitea.home.lan
      paths:
        - path: /
          pathType: Prefix
gitea:
  admin:
    username: "admin"
    password: "password"
  config:
    server:
      ROOT_URL: http://gitea.home.lan/
    service:
      DISABLE_REGISTRATION: false
      ENABLE_NOTIFY_MAIL: false
persistence:
  enabled: true
  size: 1Gi